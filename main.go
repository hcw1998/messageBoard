package main

import (
	// "fmt"
	//"messageBoard/gin/Databases"
	"messageBoard/gin/Router"
)

func main() {
	// sqlDB, err := database.DB.DB()

	// // Close
	// sqlDB.Close()
	// if err != nil {
	// 	fmt.Printf("database error %v", err)
	// }
	router.InitRouter()
}
